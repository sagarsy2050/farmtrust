[project]
name = "farmtrust-ai-image-detector"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.2.0",
    "torchvision>=0.17.0",
    "transformers>=4.40.0",
    "pillow>=10.3.0",
]

[tool.uv]
package = false
